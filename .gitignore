# secrets — never commit
.env
*.key

# real user data / local DB (the Data Trap holds PII later)
*.db
data/datatrap.db

# python
__pycache__/
*.pyc
.venv/
venv/
.coverage
htmlcov/

# os junk
.DS_Store
Thumbs.db

# NOTE: data/generated/*.jsonl and data/final/*.jsonl ARE tracked on purpose —
# the curated dataset is the asset. Only real user logs (*.db) are ignored.

# claude local permission settings - machine-specific
.claude/settings.local.json
