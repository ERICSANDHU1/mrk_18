import StudioClient from "@/components/studio/StudioClient";

export const metadata = {
  title: "Studio — your Business DNA · mrk18",
  description: "Your CMO reads your brand and builds your marketing.",
};

export default function StudioPage() {
  return <StudioClient />;
}
