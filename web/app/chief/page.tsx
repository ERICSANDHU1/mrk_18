import ChiefClient from "@/components/app/chief/ChiefClient";

/** Chief — the founder's command center: leaks, eagle view, channels, content,
 *  and the next move, grounded in real data. */
export default function ChiefPage() {
  return <ChiefClient />;
}
