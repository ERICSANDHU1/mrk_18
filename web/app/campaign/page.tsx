import CampaignStudio from "@/components/app/CampaignStudio";

/** The onboarded founder's Create-Campaigns page — one free branded campaign. */
export default function CampaignPage() {
  return <CampaignStudio />;
}
