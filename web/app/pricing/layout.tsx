import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pricing — mrk18",
  description:
    "Three ways to put the AI CMO to work: Pro, Max (with the mrk device, Founding 500 only), and Enterprise for agencies automating their clients' ads.",
};

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
